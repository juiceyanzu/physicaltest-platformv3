# 体测平台三端数据互通测试方案

## 1. 测试概述

本测试方案用于验证体测平台三端（学生端小程序、教师端网页、管理员端网页）之间的数据互通与同步功能，确保数据一致性、安全性和实时性。

## 2. 测试环境

| 环境 | URL | 账号 |
|------|-----|------|
| 后端API | http://localhost:3000/api | - |
| 学生端小程序 | 微信开发者工具 | 2026001/任意密码 |
| 教师端网页 | http://localhost:5175/teacher | T2026001/任意密码 |
| 管理员端网页 | http://localhost:5175/admin | admin/任意密码 |

## 3. 测试用例

### 3.1 用户认证测试

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| TC-001 | 学生登录小程序 | 登录成功，获取用户信息和token |
| TC-002 | 教师登录网页 | 登录成功，获取用户信息和token |
| TC-003 | 管理员登录网页 | 登录成功，获取用户信息和token |
| TC-004 | 无效凭证访问 | 返回401错误，跳转到登录页 |

### 3.2 批次管理测试

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| TC-005 | 管理员创建批次 | 批次创建成功，三端可见 |
| TC-006 | 管理员更新批次 | 批次更新成功，三端同步更新 |
| TC-007 | 管理员删除批次 | 批次删除成功，三端同步删除 |
| TC-008 | 学生查看批次列表 | 只能看到active状态的批次 |

### 3.3 预约管理测试

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| TC-009 | 学生预约体测 | 预约成功，批次人数+1，教师端可见 |
| TC-010 | 学生取消预约 | 取消成功，批次人数-1，教师端可见 |
| TC-011 | 教师标记预约完成 | 状态更新为completed，学生端可见 |
| TC-012 | 批次满员预约 | 返回错误提示"批次已满" |

### 3.4 成绩管理测试

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| TC-013 | 教师录入成绩 | 成绩录入成功，学生端可见 |
| TC-014 | 教师更新成绩 | 成绩更新成功，学生端同步更新 |
| TC-015 | 教师删除成绩 | 成绩删除成功，学生端同步删除 |
| TC-016 | 学生查看成绩 | 只能查看自己的成绩 |

### 3.5 公告管理测试

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| TC-017 | 管理员发布公告 | 公告发布成功，学生端可见 |
| TC-018 | 管理员更新公告 | 公告更新成功，学生端同步更新 |
| TC-019 | 管理员删除公告 | 公告删除成功，学生端同步删除 |

### 3.6 用户管理测试

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| TC-020 | 管理员创建学生 | 学生创建成功，可登录小程序 |
| TC-021 | 管理员更新学生 | 学生信息更新成功 |
| TC-022 | 管理员删除学生 | 学生删除成功，相关数据级联删除 |
| TC-023 | 学生修改个人信息 | 信息更新成功 |

### 3.7 权限控制测试

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| TC-024 | 学生访问教师接口 | 返回403错误 |
| TC-025 | 教师访问管理员接口 | 返回403错误 |
| TC-026 | 学生查看他人成绩 | 返回403错误或只能看到自己的成绩 |

### 3.8 数据同步测试

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| TC-027 | 多端同时访问相同数据 | 返回一致的数据 |
| TC-028 | 一端修改数据后其他端刷新 | 数据已更新 |
| TC-029 | 并发预约同一批次 | 数据一致性保持 |

## 4. 测试步骤

### 4.1 准备工作
1. 启动后端API服务：`cd backend && npm install && npm run dev`
2. 启动小程序开发服务器：`cd miniprogram && npm install && npm run dev:mp-weixin`
3. 启动网页应用开发服务器：`cd web && npm install && npm run dev`

### 4.2 执行测试
1. 按测试用例顺序执行
2. 记录每个用例的执行结果
3. 对失败的用例进行问题定位和修复

## 5. 测试工具

| 工具 | 用途 |
|------|------|
| Postman | API接口测试 |
| 微信开发者工具 | 小程序测试 |
| 浏览器控制台 | 网页调试 |
| MySQL Workbench | 数据库验证 |

## 6. 测试报告模板

```
测试日期：2026-XX-XX
测试人员：XXX
测试环境：开发环境

测试结果：
┌──────────────┬──────────┬──────────┐
│ 测试用例     │ 结果     │ 备注     │
├──────────────┼──────────┼──────────┤
│ TC-001       │ PASS     │          │
│ TC-002       │ PASS     │          │
│ ...          │ ...      │          │
└──────────────┴──────────┴──────────┘

通过率：X/X (XX%)
```

## 7. 预期测试结果

所有测试用例应通过，确保：
- ✅ 三端数据实时同步
- ✅ 权限控制正确
- ✅ 数据一致性保障
- ✅ 错误处理完善