```
Thoughts: The diagnosis clearly identifies commit a1b2c3d4 introduced an O(n²) algorithm causing CPU exhaustion and thread pool saturation. The evidence strongly correlates the commit with the performance degradation. The safest approach is to rollback this commit. I will now provide the final answer.
</code>
<code>
final_answer("Based on the diagnosis, commit a1b2c3d4 introduced an O(n²) algorithm causing CPU exhaustion (95.2% avg) and thread pool saturation (256/256). The safest and fastest fix is to immediately rollback commit a1b2c3d4 in src/queue_processor.py and src/batch_processor.py to restore the previous working version. This will eliminate the O(n²) behavior and unthrottled parallel processing that caused the CPU spike. The risk of side effects is low (temporary throughput reduction) and the probability of success is high given the strong correlation between the commit and the performance degradation.")
</code>